# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.HomeController.index

POST     /hello                      controllers.HomeController.sayHello

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

POST    /downloadExcel              controllers.FrameworkController.createExcel

POST    /framework/publish          controllers.FrameworkController.publishFramework

POST    /createOrUpdateFramework                    controllers.FrameworkController.createupdateoperation